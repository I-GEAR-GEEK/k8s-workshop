apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myapp-pv-claim
  namespace: kube-workshop
  labels:
    app: myapp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
